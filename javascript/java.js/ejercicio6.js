// Crear dos arrays con diferentes elementos
const array1 = [1, 2, 3];
const array2 = ['a', 'b', 'c'];

// Combina los dos arrays en un solo array nuevo
const arrayCombinado = array1.concat(array2);

// Imprimir el array combinado
console.log("Array combinado:", arrayCombinado);
